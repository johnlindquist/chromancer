{
  "name": "chromancer",
  "version": "1.0.0",
  "description": "Chromancer - Command-line mastery over Chrome through DevTools Protocol",
  "main": "dist/index.js",
  "bin": {
    "chromancer": "./bin/run.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepack": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["chrome", "devtools", "automation", "scraping"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@oclif/core": "^4.3.1",
    "@oclif/plugin-help": "^6.2.28",
    "@types/node": "^22.15.29",
    "oclif": "^4.18.0",
    "puppeteer-core": "^24.10.0",
    "typescript": "^5.8.3"
  },
  "oclif": {
    "bin": "chromancer",
    "dirname": "chromancer",
    "commands": "./dist/commands",
    "plugins": [
      "@oclif/plugin-help"
    ]
  },
  "files": [
    "/bin",
    "/dist",
    "/oclif.manifest.json"
  ]
}